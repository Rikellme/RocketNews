const email = document.getElementById('email').value

document.querySelector("button").addEventListener('click', () => {
    alert("Email " + document.getElementById('email').value + " enviado com sucesso !!")
})